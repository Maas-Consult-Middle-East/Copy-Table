rules:
  - id: frappe-cur-frm-usage
    languages: [javascript]
    severity: WARNING
    message: "cur_frm is deprecated and can introduce buggy behaviour — use the frm parameter passed to the event handler instead."
    fix: frm
    pattern: cur_frm
